alert();

// Variabili per il GPS

const MAX_RADIUS_METERS = 500; // distanza oltre la quale il cerchio è minimo
const MIN_SIZE = 40;           // px — dimensione minima del cerchio
const MAX_SIZE = 200;          // px — dimensione massima (quando sei sul punto)

const DISTANCE_FOUND = 5;      // px — distanza dal punto
const DISTANCE_GREEN = 40;     // px — distanza dal punto
const DISTANCE_YELLOW = 100;   // px — distanza dal punto
